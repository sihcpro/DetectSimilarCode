








int(){
scanf("%d",&long);
for(long=1;long<=long;long++)scanf("%lld",&long[long]);
for(long=1;long<=long;long++){
scanf("%lld",&long[long]);
long[long]=long[long-1]+long[long];
}


for(long=1;long<=long;long++){
long=upper_bound(long;
long[long]+=long[long-1]+long[long]-long[long-1];
priority_queue.push(long);

while(!priority_queue.empty()){
if(priority_queue.top()<=long)priority_queue.pop();
elsebreak;
}

printf("%lld ",long[long]*priority_queue.size()+long[long]);
}

return0;
}
32

